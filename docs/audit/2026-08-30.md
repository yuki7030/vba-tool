# 指示ファイル監査 2026-08-30

機械監査(`scripts/audit_instructions.py --scan .`): 47 ファイル / 指摘 0 件。
手動での横断突合による指摘: 9 件(High: 1 / Mid: 5 / Low: 2 / Info: 1)。
機械監査が 0 件でも意味的な陳腐化・重複・矛盾は検出されるため、突合は毎回必要。

| # | 重大度 | ファイル | 指摘 | 対処 |
|---|---|---|---|---|
| 1 | High | .claude/agents/Explore.md | 「導入は任意、`~/.claude/agents/` にコピー」と書きつつ、ファイル自体がプロジェクトの `.claude/agents/` にトラック済み。コピー不要で既定で組み込み Explore を上書きしており、本文が警告する副作用を全利用者が無自覚に負う | 「既定で有効」と実態に合わせて文言修正済。副作用の警告は維持し、「削除すれば組み込みに戻る」導線を追加 |
| 2 | Mid | docs/README-AI-SETUP.md | `$LINK_SKILLS` を4項目と書き写していたが実体は6項目(`diagnosing-bugs` / `writing-for-agents` 追加分が未追随) | 書き写しを削除し scripts/link-skills.ps1 への参照に置換済。「書き写さない」理由も明記 |
| 3 | Mid | AGENTS.md | 記入済みの技術スタック節に、テンプレート執筆者向け HTML コメントが残存(振る舞いを変えない無効行) | 削除済 |
| 4 | Mid | AGENTS.md「## 検証」 | 空のプレースホルダのまま。2026-07-02 監査 #5 から未解消 | 保留(要ユーザ)。実プロジェクト展開時に検証コマンドを記入するか、見出しごと削除 |
| 5 | Mid | .github/skills/diagnosing-bugs/SKILL.md | AGENTS.md「停止条件」をパラフレーズ再掲。完全一致でないため機械監査の重複検出をすり抜けていた | AGENTS.md 参照の1文へ短縮済 |
| 6 | Mid | scripts/audit_instructions.py | 行数予算がサフィックス末尾一致判定のため、素の `.md` で終わる `.claude/agents/*.md` / `.claude/commands/*.md` に効かず素通り。Explore 34行・challenger 38行等が未検知だった | `PATH_BUDGETS`(`/agents/`=40行, `/commands/`=15行)でパス部分一致のフォールバック判定を追加済 |
| 7 | Low | .github/hooks/doxygen.json, block-dangerous-bash.json | Claude 側 settings.json の matcher 絞り込み(`Edit\|Write\|MultiEdit` / `Bash`)と非対称。全ツール呼び出しで発火する可能性 | 保留。Copilot の hook スキーマが matcher を持つか本リポジトリの資料からは**未確認**。仕様確認後に判断 |
| 8 | Low | .claude/agents/instruction-auditor.md ⇔ .github/agents/instruction-auditor.agent.md | tools 表現差(Copilot 側に Write 相当が明示されていないように見える) | 保留。Copilot の `'edit'` が新規作成を内包するか**検証不能**。権限差かプラットフォーム表記差か断定できず |
| 9 | Info | README.md | `src/` `xlflow.toml` `build/vba-tool.xlsm` を前提に構成表を書いているが、いずれも実在せず git 履歴にも痕跡なし。本リポジトリがテンプレートである旨が README 自身に未記載 | 保留。instruction-audit の直接対象外として静観 |

総評: 機械監査は 0 件を維持しているが、それは指摘が無いことではなく検出器の射程外だったことを意味する場合がある(#5 は完全一致ベースの重複検出をすり抜け、#6 は行数予算そのものの穴)。
#6 の修正で `.claude/agents/` 配下が予算判定の対象に入った。ただし `/agents/` の 40 行は既存最大 38 行(challenger.md)のすぐ上に置いた値であり、既存分を許容する方針に沿った「これ以上太らせない」ストッパーとして機能する。既存の分量を圧縮する働きはない。

Doxygen 規則の3層重複は 2026-07-02 監査で「ロード契機が異なる段階的要約として許容」と裁定済みのため、今回は再指摘していない。
