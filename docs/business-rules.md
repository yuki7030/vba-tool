# 業務ルール

AIへ: 計算・判定の仕様はここが正。一般常識で補完しない。特に「例外」を必ず確認する。

## 計算ルール
| ルール | 原則 | 例外 |
|---|---|---|
| 消費税 | 明細単位・切り捨て(例) | 得意先9xxxは伝票単位・四捨五入(例) |

## 日付・締めルール
<!-- 締め日、営業日判定、年度の起点(4月始まり等) -->

## 状態遷移・制約
<!-- 例: 承認済み伝票は編集不可、削除は赤伝発行のみ -->

## 運用ルール
- 本ファイルに置くのは**複数機能に跨るルールだけ**。単一機能で閉じる判定ルールは
  その機能の docs/spec/features/FEAT-*.md に書き、必要なら FEAT 側から本ファイルへリンクする。
- 正本(FEAT)で確定したルールが横断的だと判明したら、本ファイルへ昇格転記して FEAT からリンクに置き換える。
- 一般則よりも例外・特殊ケースを優先して書く(AIは一般則を推測できるが例外は知り得ない)。
